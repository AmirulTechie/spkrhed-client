// TODO: replace with the real production domain once it's finalized, or set
// NEXT_PUBLIC_SITE_URL in the deployment environment — everything that needs
// an absolute URL (metadataBase, sitemap.xml, robots.txt) reads from here.
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || "https://spkrhed.com";

export const SITE_NAME = "SPKRHED";
